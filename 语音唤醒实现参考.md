# 结论先行

有。除 Vosk（`long3.py` / `long4.py`）之外，在 **2K3000 开发板 + Loongnix_v25 桌面版** 上**真正可落地**的替代方案主要有 **3 个**：

1. **openWakeWord + ONNX Runtime**（**最推荐的“专用语音唤醒”方案**）  
2. **PocketSphinx（CMU Sphinx）关键词检索 KWS**（最轻量、可 `apt` 装）  
3. **Whisper.cpp**（替代 Vosk 的轻量 ASR，免编译 Kaldi）

而 **Snowboy / Porcupine（Picovoice）/ 讯飞 / 百度 / 阿里等商业或闭源唤醒 SDK 没有 LoongArch 构建，在 2K3000 上不可用**。下面按“能不能用、怎么用”展开。

---

## 1. 先确认 2K3000 + Loongnix_v25 的底子

- **2K3000 是 8 核 LA364E 处理器**，主频 ≥2.0GHz（部分 SKU 可达 2.2–2.5GHz），每核 64KB L1、共享 6MB L2，集成第二代自研 LG200 GPU（≥6 TOPS INT8，部分资料称峰值 8 TOPS）。  
  开发板内存常见 **8GB 起**，BPI-2K3000 可选 8/16/32GB DDR4，OrangePi Nova 最高 32GB；也有 4GB/8GB LPDDR4X 的工控配置 。
- **Loongnix 25.1.rc1 基于 Debian 13（trixie）**，官方支持 2K3000 平台，系统 Python 为 **3.13.5**、gcc-14，桌面为 KDE Plasma（也可换 XFCE）；内核已修复 ALSA 相关问题，`arecord` / alsa-utils 可正常使用 。

**判断**：8 核 LA364E + 数 GB 内存在 LoongArch 里算“够用但不宽裕”，跑轻量唤醒/ASR 完全没问题，但要避开 GB 级大模型和重型编译（如完整 Kaldi）。

---

## 2. 三个可落地方案对比

| 方案 | 类型 | 2K3000 / Loongnix_v25 可用性 | 资源占用 | 自定义中文唤醒词 | 适合场景 |
|---|---|---|---|---|---|
| **openWakeWord + ONNX Runtime** | 专用唤醒词检测（非 ASR） | ✅ **可用**。ONNX Runtime 自 **1.17.0 官方支持 LoongArch** ；龙芯 PyPI 有 `onnxruntime` **1.22.2 cp313 manylinux_2_38_loongarch64** wheel，也有 1.15.1 loongarch64 wheel ；`tflite-runtime` 也有 loongarch 包 。openWakeWord 在 Linux 上用 onnxruntime/tflite 推理  | 极小：自训练 ONNX 模型约 **200KB**，输入 16kHz PCM，CPU 实时无压力  | ✅ 支持。用 `language="zh"` 合成/录制正样本训练，产出 `.onnx`  | **首选**：低延迟专用唤醒；训练在 PC/Colab 做，板上只推理 |
| **PocketSphinx（CMU Sphinx）** | 关键词检索 KWS + 有限语法指令识别 | ✅ **可用**。Debian loong64 已有 `python3-sphinxbase`、`libsphinxbase3t64`、`libpocketsphinx-dev` 的 loong64 构建 ；PyPI 官方 `pocketsphinx` 5.1.1 支持 Python 3.13  | 很轻：传统 GMM 模型，中文模型几十 MB，内存/CPU 占用极低 | ⚠️ 需下载 Mandarin 声学模型+拼音词典，把“小龙小龙/晓珑监护”做成 KWS 关键词表或 JSGF 语法；准确率低于神经网络 | 单引擎搞定“唤醒+指令”，最省资源，适合工控/嵌入式 |
| **Whisper.cpp** | 量化 Whisper ASR（录音→转写→文本匹配，思路同 `long4.py`） | ✅ **可用**。纯 C/C++ 零依赖，AOSC 已提供 **whisper.cpp 1.9.2 的 loongarch64 包** ；龙芯上 `cmake` 编译即可。支持 `tiny/base` 量化（`tiny` 75MB / 内存约 273MB；`base` 142MB / 约 388MB） | 中等：内存几百 MB，需 3–5 秒轮询转写，有延迟 | ✅ Whisper 多语言模型原生支持中文，直接用文本匹配“小龙小龙”等 | 想保留原“ASR 文本匹配”逻辑、且想要更好中文识别率时，替代 Vosk |
| Vosk（上文 `long4.py`） | 流式/文件 ASR | ⚠️ 仍可用，但需**自行编译 Kaldi + vosk-api**，用 `vosk-model-small-cn-0.22`；编译重、实时性一般 | 中等 | ✅ 好 | 保底方案；8 核上能跑，但不如上面轻量 |
| Snowboy / Porcupine / 讯飞 / 百度 / 阿里 SDK | 商业或闭源唤醒 | ❌ **不可用**：无 LoongArch 预编译库/SDK 发布 | — | — | 排除 |

> 补充：openWakeWord 的 **Speex 噪声抑制**目前只支持 X86 / Arm64 Linux ，在 loong64 上不可用；但噪声抑制是可选增强，不影响基础推理，可省略或用其它降噪替代。

---

## 3. 推荐落地组合

### 组合 1（最佳实时唤醒，最推荐）：openWakeWord 唤醒 + PocketSphinx/Whisper 做指令

1. **在 x86 PC 或 Colab 上训练自定义中文唤醒词**  
   用 openWakeWord 的中文示例流程，对“小龙小龙”“晓珑监护”生成正样本（Piper TTS 合成或真人录音）+ 环境负样本，训练后导出 `my_wakeword.onnx`（约 200KB）。训练依赖 PyTorch/torchaudio，建议在带 GPU 的机器上做；龙芯上虽然也有 `torch` loongarch64 wheel，但在 2K3000 上训练太慢，不推荐在板上训 。

2. **2K3000 板上部署推理**  
   - `python3 -m venv` 建虚拟环境，用龙芯官方脚本配置 pip 源（`set-pip.sh`，glibc≥2.38 走 `lpypi.loongnix.cn`）。  
   - `pip install openwakeword`：在 loong64 上会自动从龙芯源拉 **onnxruntime loongarch64 wheel**（确认 Python 3.13 对应 `cp313` 标签）。  
   - 采集音频：用 `arecord -f S16_LE -r 16000 -c 1 -t raw` 通过管道读出 16-bit 16kHz PCM，按 **80ms 帧**送 `oww.predict(frame)`；命中即进入唤醒态 。这样连 PyAudio 都不需要。

3. **唤醒后的指令识别**  
   - 要最省：用 **PocketSphinx** 的 JSGF 语法/关键词识别“切换到午睡模式”“关闭告警”等固定指令（`apt install pocketsphinx python3-pocketsphinx`）。  
   - 要识别率更好：唤醒后再录 2–3 秒，用 **Whisper.cpp tiny** 转写，再做你原来的 `handle_command` 文本匹配。  

   ✅ 这套完全**不需要编译 Vosk/Kaldi**，唤醒是专用模型，误唤醒低、CPU 占用极小，最契合 2K3000。

### 组合 2（最省事、单引擎）：全 PocketSphinx KWS

- `apt install pocketsphinx python3-pocketsphinx`（Loongnix 源若源自 Debian loong64，则已有对应包）。
- 下载 CMU 的 **中文（Mandarin）声学模型 + 拼音词典**，写 `keywords.txt`，把“小龙小龙”“晓珑监护”以及“关闭告警 / 切换到午睡模式 / 查询温湿度 …”都作为关键词并配阈值。
- 运行 `pocketsphinx_continuous -inmic yes -kws keywords.txt`，或用 Python `LiveSpeech(keyphrase=..., kws_threshold=...)` 。  
  ✅ 一个进程搞定“唤醒 + 指令”，CPU/内存最省，最适合 2K3000 的工控/嵌入式定位；代价是要花时间配中文词典，且识别准确率不如神经网络方案。

### 组合 3（最小改动保留原逻辑）：Whisper.cpp 替换 Vosk

- 保留 `long4.py` 的 `arecord` 录音 → `clean_text` → `handle_command` 文本匹配框架，**只把 `from vosk import ...` 那段换成 Whisper 转写**。
- 板上编译 whisper.cpp（`cmake -B build && cmake --build build -j --config Release`，纯 C/C++ 零依赖）或直接装 AOSC 的 loongarch64 包 ；下载 `ggml-tiny` / `ggml-base` 的量化模型 。
- 用 `whisper-cli` 转写 3–5 秒 WAV，或部署 `whisper-stream` 做近实时流式 。  
  ✅ 中文识别率好于 PocketSphinx，编译比 Vosk 简单太多；缺点是有转写延迟、内存占用比专用 KWS 大。

---

## 4. 上板注意事项

- **Python 环境**：Loongnix 25 系统 Python 受 PEP 668 保护，全局 `sudo pip install` 会被拒；务必 `python3 -m venv`，并用龙芯 `set-pip.sh` 配置源 。
- **音频采集**：优先用 `arecord` 原始 PCM 管道（Loongnix 25 已修 ALSA ），避免强依赖 PyAudio；若用 openWakeWord 官方麦克风示例才需 `libportaudio2`（loong64 有包）。
- **TTS**：你原代码用的 `espeak` 在 loong64 有包，可继续用；若 `espeak -v zh` 中文异常，改装 `espeak-ng` 并手动验证 `espeak-ng -v zh "测试"`。
- **若坚持 Vosk**：仍按上轮结论——2K3000 上选 `long4.py` + 自编译 vosk + `vosk-model-small-cn-0.22`；8 核编译 Kaldi 耗时较长，且综合体验不如上面的 openWakeWord / Whisper.cpp 方案轻量。

---

## 一句话总结

**2K3000 + Loongnix_v25 上，首选 `openWakeWord`（ONNX Runtime 龙芯官方支持，训练自定义中文唤醒词，推理极轻）做“小龙小龙/晓珑监护”唤醒，再配 `PocketSphinx`（最省）或 `Whisper.cpp`（识别率好）做指令识别；想单引擎最省事就用 PocketSphinx KWS，想最小改动保留原逻辑就用 Whisper.cpp 替掉 Vosk。商业/闭源唤醒 SDK 在 LoongArch 上不可用。**